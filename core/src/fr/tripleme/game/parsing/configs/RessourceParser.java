package fr.tripleme.game.parsing.configs;

import fr.tripleme.game.parsing.Parser;

public class RessourceParser extends Parser {
    /**
     * @param path Path of the json to parse.
     */
    public RessourceParser(String path) {
        super(path);
    }
}
